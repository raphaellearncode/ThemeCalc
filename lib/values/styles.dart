import 'package:flutter/material.dart';

const kTitleTextStyle =
    TextStyle(color: Colors.white, fontSize: 25.0, fontFamily: 'Rjd');

const kWrittenTxtStyle =
    TextStyle(color: Colors.white, fontSize: 35.0, fontFamily: 'Written');

const kBoxShadow = BoxShadow(
  color: Colors.black54,
  blurRadius: 10.0,
  offset: Offset(0.0, 10.0),
);
